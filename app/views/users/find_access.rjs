case 
  when @value == 'Carrier'
    page.replace_html :user_options, :partial => 'shipper_options', :object => @shippers
  when @value == 'Winery'
    page.replace_html :user_options, :partial => 'winery', :object => @wineries
  when @value == 'nil'
    page.replace_html :user_options, ""
end
  